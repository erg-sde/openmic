<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "OPEN MIC", root_path , id: "logo", class: 'navbar-brand' %>
      <%= link_to "Events", events_path , id: "logo", class: 'navbar-brand' %>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
        <% if logged_in? %>
          <li><%= link_to "Account", current_user, class: 'active'%></li>
        <% elsif venue_logged_in?%>
          <li><%= link_to "Venue Account", current_venue %></li>
          <li><%= link_to "Add an event", newevent_path %></li>
        <%else %>
        <% end %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if @user.present? %>
          <li>
            <a href="#">
              <%= @user.email %>
            </a>
          </li>
        <% end %>
        <% if @venue.present? %>
          <li>
            <a href="#">
              <%= @venue.email %>
            </a>
          </li>
        <% end %>
        <li>
          <% if logged_in? %>
          <li><%= link_to "Log out", logout_path, method: :delete %> </li>
          <% elsif venue_logged_in?%>
          <li><%= link_to "Log out", venue_logout_path, method: :delete %> </li>
          <% else %>
            <!-- <%= link_to login_path do %>
              <i class="fa fa-sign-in fa-2x" aria-hidden="true"></i> -->
            <%end%>
          <%end%>
        </li>
      </ul>
    </div><!--/.nav-collapse -->
  </div><!--/.container-fluid -->
</nav>
